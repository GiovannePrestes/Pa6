@model ControlePatrimonios.Models.TbEncerramento

@{
    ViewData["Title"] = "Editar";
    var message = ViewBag.Message as ControlePatrimonios.Models.Message;
}

@if (message != null)
{
    @if (message.Icon.Equals("success"))
    {
        <script>
        swal('@message.Title', '@message.Text', '@message.Icon').then(() => { window.location.href = "@Url.Action("Index", "Encerramento")"; });
        </script>
    }
    else
    {
        <script>
        swal('@message.Title', '@message.Text', '@message.Icon');
        </script>
    }
}
<h2>Editar Encerramento</h2>

<hr />
<div class="center">
    <div class="row">
        <div class="col-md-offset-4 col-md-4">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="IdEncerramento" />
                <div class="form-group">
                    <label asp-for="IdItemNavigation" class="control-label"></label>
                    <select asp-for="IdItem" class="form-control" asp-items="ViewBag.IdItem"></select>
                    <span asp-validation-for="IdItem" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Motivo" class="control-label"></label>
                    <input asp-for="Motivo" class="form-control" />
                    <span asp-validation-for="Motivo" class="text-danger"></span>
                </div>
                <div class="form-group" align="right">
                    <input type="submit" value="Salvar" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>

    <div class="col-md-offset-4 col-md-4">
        <a asp-action="Index">Voltar para a lista</a>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
